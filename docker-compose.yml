services:
  bot:
    image: yt-dlp-tg-bot
    container_name: bot
    build: .
    env_file:
      - config.inc.sh
    volumes:
      - ./yt-dlp-cookies.txt:/app/yt-dlp-cookies.txt
      - ./yt-dlp.conf:/root/yt-dlp.conf
    restart: always
